# Application name
spring.application.name=mining

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/miningindaba
spring.datasource.username=postgres
spring.datasource.password=Margot+250793833800
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Server port (optional, default is 8080)
server.port=8080

# Multipart file upload configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.file-size-threshold=2KB

# --- SMTP Email Configuration (replace with your actual values) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=margotgloire@gmail.com
spring.mail.password=snkr imna jjcx eimw
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.default-encoding=UTF-8
# spring.mail.properties.mail.smtp.ssl.trust=smtp.example.com
# spring.mail.properties.mail.smtp.ssl.enable=true # Uncomment for SSL (if needed)